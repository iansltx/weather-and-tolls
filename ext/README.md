# kiosk Extension

Auto-generated PHP extension from Go code.

## Functions

### kiosk_resolve_coords

```php
kiosk_resolve_coords(string $location): array
```

**Parameters:**

- `location` (string)

**Returns:** array

### kiosk_fetch_weather

```php
kiosk_fetch_weather(float $lat, float $lon): array
```

**Parameters:**

- `lat` (float)
- `lon` (float)

**Returns:** array

### kiosk_fetch_tolls

```php
kiosk_fetch_tolls(): array
```

**Returns:** array

### kiosk_mercure_subscriptions

```php
kiosk_mercure_subscriptions(): array
```

**Returns:** array

### kiosk_mercure_publish

```php
kiosk_mercure_publish(string $topic, string $data, string $type, string $id): array
```

**Parameters:**

- `topic` (string)
- `data` (string)
- `type` (string)
- `id` (string)

**Returns:** array


